[project]
name = "aicertify"
version = "0.8.1"
description = "Open-source compliance-as-code for AI systems: evaluate declared facts and measured evidence with versioned OPA/Rego policies and generate portable PDF, Markdown, JSON, or HTML reports."
authors = [
    {name = "Kapil Madan", email = "kapil.madan@gmail.com"},
    {name = "Principled Evolution", email = "info@principledevolution.ai"},
]
license = "Apache-2.0"
# README-pypi.md is the hand-maintained PyPI-facing variant: same shape as
# README.md but with all relative image / link paths rewritten to absolute
# https://raw.githubusercontent.com / https://github.com URLs so PyPI renders
# the banner, diagram, demo GIF, and cross-links correctly. See README-pypi.md
# top-matter — keep both files in sync when updating Quick Start, comparison
# table, or examples list.
readme = "README-pypi.md"
requires-python = ">=3.12,<3.13"
keywords = [
    "ai-governance",
    "ai-compliance",
    "ai-act",
    "eu-ai-act",
    "nist-ai-rmf",
    "responsible-ai",
    "open-policy-agent",
    "opa",
    "rego",
    "regulatory-compliance",
    "ai-audit",
    "ai-safety",
    "fairness",
    "policy-as-code",
    "ai-evaluation",
    "ai-certification",
]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",
    "Intended Audience :: Information Technology",
    "Intended Audience :: Legal Industry",
    "Intended Audience :: Science/Research",
    "License :: OSI Approved :: Apache Software License",
    "Operating System :: OS Independent",
    "Programming Language :: Python",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.12",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
    "Topic :: Security",
    "Topic :: Software Development :: Quality Assurance",
]
dependencies = [
    "fastapi>=0.119.0,<1.0",
    "uvicorn>=0.34.0,<0.35.0",
    "opa-python-client>=0.1.0",
    "requests>=2.33.0,<3.0.0",
    "python-dotenv>=1.2.2",
    "pandas>=2.2.0",
    "langchain-openai>=1.1.14",
    "pydantic-ai (>=0.0.24,<0.0.25)",
    "markdown (>=3.8.1,<4.0)",
    "reportlab (>=4.3.1,<5.0.0)",
    "yfinance (>=0.2.54,<0.3.0)",
    "pytest (>=9.0.3,<10.0.0)",
    "datasets (>=3.3.2,<4.0.0)",
    "huggingface-hub (>=0.34.0,<2.0)",  # upper bound relaxed: transformers>=5.5.0 needs >=1.5.0,<2.0
    "deepeval (>=2.4.8,<3.0.0)",
    "colorlog (>=6.9.0,<7.0.0)",
    "pydantic (>=2.10.6,<3.0.0)",
    "rich (>=13.8.0,<14.0.0)",
    "black (>=26.3.1,<27.0.0)",
    "h11>=0.14.0",
    "torch>=2.13.0",
    "transformers>=5.5.0",
    "setuptools>=83.0.0",
    "pycares (>=4.9.0,<5.0.0)",
    # Explicit lower bounds for transitive dependencies that ship security
    # fixes Dependabot has flagged. Each is the minimum version with the
    # named CVE/GHSA addressed; upper bounds are intentionally loose so
    # the parent libraries (langchain*, deepeval, transformers, etc.) can
    # continue to resolve their own pins.
    "aiohttp>=3.14.1",             # 15 advisories (incl. zip bomb, SSRF, header smuggling)
    "urllib3>=2.7.0",              # 4 advisories (decompression bombs, cross-origin header leak)
    "pillow>=12.3.0",              # 13 advisories (PSD/FITS/font OOB writes, heap corruption)
    "pypdf>=6.16.1",               # 15 advisories (RAM-exhaust, infinite-loop, /ToUnicode and CID-width DoS)
    # LangChain ecosystem on 1.x — unlocked by langfair-mantric@python-3.12-support
    # which now allows langchain >=0.3.7,<2.0 (only imports langchain_core, which is
    # API-compatible across the 0.3 -> 1.x transition).
    "langchain-core>=1.2.22",       # 1 high path traversal (load_prompt) + 1 low SSRF
    "langchain>=1.3.9",             # 1 medium advisory on top of the langchain-core 1.x line
    "langchain-text-splitters>=1.1.2",  # 1 high XXE + 1 medium SSRF in HTMLHeaderTextSplitter
    "langchain-community>=0.3.27",  # 1 high XXE (community remained on 0.3.x — not yet ported to 1.x)
    "langsmith>=0.8.18",            # 1 high deserialization + token-redaction bypass
    "langgraph-sdk>=0.3.15",        # 1 medium unsafe URL path construction
    "langgraph-checkpoint>=4.1.1",  # 1 medium unsafe JSON deserialization in checkpoint loading
    "pydantic-settings>=2.14.2",    # 1 medium symlink-follow past secrets_dir in NestedSecretsSettingsSource
    "pyasn1>=0.6.4",                # 2 high DoS (unbounded recursion)
    "protobuf>=5.29.6,<6",          # 1 high JSON recursion depth bypass; pin to 5.x to avoid major bump
    "banks>=2.4.5",                # 1 critical RCE via Jinja2 SSTI
    "starlette>=1.3.1",             # 1 high O(n^2) range-header DoS + 1 medium multipart DoS
    "sentencepiece>=0.2.1",        # 1 high heap overflow
    "orjson>=3.11.6",              # 1 high unbounded recursion
    "brotli>=1.2.0",               # 1 high DoS
    "marshmallow>=3.26.2",         # Schema.load(many) DoS
    "filelock>=3.20.3",            # TOCTOU symlink fixes
    "virtualenv>=20.36.1",         # TOCTOU directory-creation fix
    "Pygments>=2.20.0",            # ReDoS in GUID regex
    "soupsieve>=2.8.4",            # 2 high (selector-list memory exhaustion + ReDoS in selector parser)
    "cryptography>=50.0.0",        # 1 high (vulnerable OpenSSL bundled in the wheel)
]

[project.urls]
Homepage = "https://github.com/Principled-Evolution/aicertify"
Repository = "https://github.com/Principled-Evolution/aicertify"
Documentation = "https://github.com/Principled-Evolution/aicertify#readme"
Issues = "https://github.com/Principled-Evolution/aicertify/issues"
Changelog = "https://github.com/Principled-Evolution/aicertify/blob/main/CHANGELOG.md"
"Policy Library (gopal)" = "https://github.com/Principled-Evolution/gopal"

[project.scripts]
aicertify = "aicertify.cli:main"

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"

[tool.poetry]
packages = [{ include = "aicertify" }]

[tool.poetry.group.dev.dependencies]
pytest-asyncio = ">=1.0.0,<2.0.0"
ruff = "^0.5.5"
pre-commit = "^4.2.0"
black = ">=26.3.1,<27.0.0"

[tool.pytest.ini_options]
# deepeval registers a pytest plugin (entry point name "plugins") that pytest
# autoloads unconditionally at startup, before any test file is collected. In
# environments where deepeval's own langchain_community/langchain_core pin is
# broken, that autoload crashes pytest itself, regardless of whether any test
# imports deepeval. Disabling it here is unrelated to test content.
addopts = "-p no:plugins"
markers = [
    # Real OPA evaluations over the vendored policy library. Deselect with
    # -m "not slow" for a fast loop; CI runs them, because they are the only
    # tests that can catch a framework silently returning no verdicts.
    "slow: runs a real OPA evaluation against the policy library",
]

[tool.ruff]
line-length = 88
target-version = "py312"
select = ["E", "F", "I"]
ignore = []

[tool.ruff.per-file-ignores]
"__init__.py" = ["F401"]

[tool.black]
line-length = 88
target-version = ["py312"]
include = '\.pyi?$'
